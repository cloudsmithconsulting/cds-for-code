<% $this.parameters['Generator'].attributes.forEach((a, i) => { %>"<%=a.DisplayName.UserLocalizedLabel.Label%>"<% if (i !== $this.parameters['Generator'].attributes.length - 1) { %>,<% } });%>
<% const records = $this.parameters['Generator'].generate($this.parameters['Count']); 
records.forEach(r => {
    const keys = Object.keys(r);
    keys.forEach((k, i) => { %>"<%=r[k]%>"<% if (i !== keys.length - 1) { %>,<% }; }); %>
<%}); %>